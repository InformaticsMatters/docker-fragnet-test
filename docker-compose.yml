version: '3.0'

services:

  graph:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        from_tag: ${FROM_TAG:-3.5}
    image: informaticsmatters/fragnet-test:3.5
    container_name: fragnet-test
